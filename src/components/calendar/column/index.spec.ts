import { CalendarColumnDumb } from '@components/calendar/column/index.dumb';

describe('CalendarColumn', () => {
  it('should be defined', () => {
    expect(CalendarColumnDumb).toBeDefined();
  });
});
