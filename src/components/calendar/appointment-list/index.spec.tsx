import { AppointmentList } from '@components/calendar/appointment-list/index';

describe('AppointmentList', () => {
  it('should be defined', () => {
    expect(AppointmentList).toBeDefined();
  });
});
